import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
        main: '#DDC6B6', 
        light: '#DDC6B6',
        dark: '#262223',
    },
    secondary: {
        main: 'rgb(23, 123, 120)',
    },
  },
});